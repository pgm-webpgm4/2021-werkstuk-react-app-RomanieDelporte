import React, { } from 'react';
import Admin from '../admin';

const AdminPage = ({children}) => {
  return (
      <div> 
          <div>Admin</div>
          <Admin/>
      </div>
   

  );
};

export default AdminPage;